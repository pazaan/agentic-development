---
description: Spawn memory-consolidator to scan memory directory for duplicates / stale entries.
---

Invoke the `memory-consolidator` subagent via the Agent tool. Pass the current project's memory directory as scope. Return the consolidation proposal; do not apply changes without user approval.
