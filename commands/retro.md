---
description: Spawn retro-writer for a 3-line retrospective on the most recently merged ticket.
---

Invoke the `retro-writer` subagent via the Agent tool. Pass the most recent ticket's plan note + completed-tasks list as context. Return the 3-line retro plus any draft role-doc delta.
